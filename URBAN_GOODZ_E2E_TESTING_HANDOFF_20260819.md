# Urban Goodz Master E2E Testing — Handoff (2026-08-19, evening)

Continuing this: paste the user's original "URBAN GOODZ MASTER END-TO-END PRODUCTION TESTING" directive (or just say "continue the E2E testing handoff") and point Claude at this file.

## Repositories in play

- **Backend/Admin**: `C:\Users\D'Andre Good\Documents\GitHub\AdminPanel_Update_V39`, branch `adminpanel-v39-backend-sprint`
- **Mobile (Customer+Vendor+Driver coherent set)**: `C:\UG\claude-order-anywhere-card-driver`, branch `claude-driver-order-anywhere-card-completion-20260728`
  - Customer = repo root (`sixam_mart`)
  - Vendor = `vendor_app/` (`urban_goodz_vendor`)
  - Driver = `driver_app/` (`urban_goodz_driver`)
  - **Do not use** `Documents\GitHub\UrbanGoodz2026-Revised` (branch `customer-tester-build-sprint`) as the mobile test environment — it only has the Customer app (no driver_app, gutted vendor_app). It's the source of a *newer* Digital Human implementation to reconcile later, not the execution environment.

## Environment state (as of last check)

- **MySQL** (Laragon, `C:\laragon\bin\mysql\mysql-8.4.3-winx64\bin`, user `root`, no password, port 3306): running. **Do not use Laragon's own start/watchdog** — it has a boot-loop bug on this machine (kills mysqld mid-crash-recovery). If it dies, kill all `mysqld.exe`, then launch manually: `cd` into the mysqld bin dir and run `./mysqld.exe --defaults-file="C:\laragon\bin\mysql\mysql-8.4.3-winx64\my.ini"` in the background, and just wait (recovery can take several minutes) — do **not** let anything restart it mid-recovery.
- **Backend server**: must be started as `php artisan serve --host=127.0.0.1 --port=8000 --no-reload`. The `--no-reload` is **required** — without it, Laravel's `ServeCommand` strips almost all env vars from the child process on this machine and `DB_*` config silently falls back to Laravel's stock defaults (`forge`/`forge`), causing every DB-touching page to 500.
- **OPcache**: enabled in `C:\laragon\bin\php\php-8.3.30-Win32-vs16-x64\php.ini` (was off by default; every request recompiled the whole app, 10-50s/page). Now ~1-6s/page.
- **Laravel Debugbar**: disabled via `.env` (`DEBUGBAR_ENABLED=false`) — its JS asset route hung indefinitely and blocked page `load` events in a real browser/Playwright.
- **App DB**: `urban_goodz_local` — fully migrated (all migrations "Ran", including one new one, see below). This is the **working app database** — do not run destructive PHPUnit `RefreshDatabase` against it.
- **PHPUnit-isolated DB**: `urban_goodz_phpunit_test` — a `mysqldump` clone of `urban_goodz_local`, fully migrated, safe to let PHPUnit tear up. `.env.testing` already points at it (`DB_USERNAME=root`, no password). Do not point `.env.testing` back at `urbangoodz_bigpickle_e2e_migrated` — that DB's migration ledger is broken (115 "pending" migrations that collide with already-existing tables; same "SQL-imported, ledger not backfilled" issue documented in the `verifying-migrations-locally` memory).

## Test accounts (all created via `php artisan tinker`, safe local-only, never real accounts)

| Role | Email | Password | Notes |
|---|---|---|---|
| Admin | `admin_test@urbangoodz.test` | `TestPass123!` | `role_id=1`, id 124. Login is at `/login/admin`, POSTs to `/login_submit` (not `/admin/auth/login`), and requires a custom image captcha whose phrase is read from the session file — see `tests/playwright/helpers/sessionCaptcha.js`. |
| Business (regular) | `business_test@urbangoodz.test` | `TestPass123!` | Attached to `UrbanGoodzBusinessClient` id 1 ("Smoke Test Company"), `role=owner_admin`. Login at `/business/login`, no captcha. |
| Dispatcher | `dispatcher_test@urbangoodz.test` | `TestPass123!` | Attached to a **separate** `UrbanGoodzBusinessClient` id 2 ("Dispatch Test Co", `account_type=dispatch_company`), `role=dispatch_owner` (only role with implicit full dispatch permissions — other roles need explicit `permissions` array entries). Same `/business/login` form; redirects to `/business/dispatcher/dashboard` based on account_type+role. |

No CUSTOMER_TEST / VENDOR_TEST / DRIVER_TEST mobile accounts created yet — that's part of the remaining mobile work below.

## What's done and verified (real, re-runnable evidence)

### Playwright — Admin/Business/Dispatcher backend (27/27 passing)
Run with: `cd AdminPanel_Update_V39 && npx playwright test` (needs backend server running per above).

Files (all real assertions now, not `expect(page).toBeDefined()` stubs):
- `tests/playwright/admin-portal.spec.js` — auth, vendor list, driver list, customer list, order list (all statuses), order details (9 tests)
- `tests/playwright/business-portal.spec.js` — auth, dashboard, package pool/scan, invoices, logout (7 tests)
- `tests/playwright/dispatcher-portal.spec.js` — auth, dashboard, loads, commissions, cross-account-denial (5 tests)
- `tests/playwright/cross-role-e2e.spec.js` — admin-side reachability of AI ops/dispatch/load-board/load-sourcing/store-list/AI-chief-of-staff (6 tests). **Note**: these do NOT drive the actual mobile Customer→Vendor→Driver chain yet — that's explicitly flagged in the file's own header comment as the next extension point.
- `tests/playwright/helpers/sessionCaptcha.js`, `tests/playwright/helpers/dbLookup.js` — local-only test helpers (read session files / shell out to `mysql` CLI for real data lookups instead of scraping AJAX-populated tables).
- `playwright.config.js` — fixed to scope `testDir` to `tests/playwright` and ignore `.claude/**` (was picking up a stale duplicate spec file from another lane's nested worktree at `.claude/worktrees/agent-ac0197f3910cb6283/`).

Known flaky (not a bug): occasionally one test times out when run immediately after many others in the same sequential run (Windows session-file I/O contention). Always passes in isolation. If you see a lone timeout in a big run, rerun just that test with `--grep` before treating it as real.

### PHPUnit (1077 passed, 2 skipped, 0 failures)
Run with: `cd AdminPanel_Update_V39 && php vendor/bin/phpunit` (uses `.env.testing` → `urban_goodz_phpunit_test`, safe).

## Real bugs found and fixed this session (backend)

1. `php artisan serve` env-stripping → `--no-reload` required (see above).
2. Laravel Debugbar hung → disabled.
3. OPcache off → enabled.
4. `reviews` table was missing `delivery_man_id` column — the original `create_reviews_table` migration is guarded by `if (!Schema::hasTable(...))` and was a no-op on this pre-imported DB, so the column silently never got added despite the migration showing "Ran". Caused a real 500 on `/admin/users/delivery-man`. Fixed with new migration `database/migrations/2026_08_19_200000_add_delivery_man_id_to_reviews_table.php`.
5. **6 wrong route-name references** causing real 500s: `BusinessAuthController::login()` redirected to `route('dispatcher.dashboard')` (doesn't exist) instead of `route('business.dispatcher.dashboard')`; `DispatcherPortalController` had 5 more instances of the same `dispatcher.*` → should be `business.dispatcher.*` pattern (in `loads.show` ×2, `territory`, `users` ×2). All fixed via the correct route names (confirmed against real `php artisan route:list` output, not guessed).
6. MySQL boot-loop — see environment notes above.
7. **`PersonaRegistry::get()` string aliases were backwards**: `'skylar' => chiefOfStaff()`, `'monique' => concierge()` — exactly opposite of the correct mapping. Fixed to `'monique' => chiefOfStaff()`, `'skylar' => concierge()`. This alias path isn't called by any production code (grepped), so it was a dormant landmine, not live-traffic. Also fixed 3 PHPUnit tests that asserted the same backwards mapping (`DigitalHumanPlatformTest`, 2× in `UrbanGoodzEcosystemEndToEndTest`). **The correct mapping (Skylar=customer-facing/concierge, Monique=business-facing/chief-of-staff) is now independently re-confirmed** against the canonical Flutter source (`UrbanGoodz2026-Revised`'s `lib/features/digital_human/core/personality_profile.dart`) and the backend config (`config/urban_goodz_personas.php`), and written up in the `ai-persona-monique-skylar` memory file — do not re-flip this again without very strong new evidence.

## In progress / blocked

- **Physical device**: the Moto G (`ZT42268MG6`) that was connected earlier in this session is **no longer showing** in `adb devices` (checked via `C:\Users\D'Andre Good\AppData\Local\Android\Sdk\platform-tools\adb.exe` — `adb` is not on PATH in Git Bash, use the full path). This blocks all Appium/physical-device work. **User action needed**: reconnect USB / re-enable USB debugging authorization.
- **Customer APK build**: was running in the background (`flutter build apk --debug` from `C:\UG\claude-order-anywhere-card-driver`) when this handoff was written — check `flutter build apk --debug` output / whether `build/app/outputs/flutter-apk/app-debug.apk` exists and is fresh before assuming it's done. Vendor/Driver apps already confirmed `flutter analyze` clean (0 errors; Vendor 0 lint issues, Driver 24 cosmetic lint issues, Customer 206 cosmetic lint issues) and `flutter pub get` clean, but have **not** been built to APK yet.
- **Digital Human trace (code-level only so far, in the coherent mobile worktree — NOT the newer branch)**:
  - Architecture: `DigitalHumanController` (state: idle/speaking/listening/thinking/gesture) → `DigitalHumanAvatarWidget` → falls back to a **painted 2D avatar**, not real Rive character animation, because **zero `.riv` files exist anywhere in this worktree** (confirmed via full-repo search). The code is honest about this — `RiveAssetManager.isAssetAvailable()` explicitly checks and falls back gracefully, no fabricated/placeholder binaries, no crash.
  - **BREATHING**: real, verified in code — continuous `AnimationController` (`_breathing`) driving a sine-wave `Transform.scale`.
  - **SPEAKING**: real, verified in code — `_pulse` AnimationController drives a visible glow-ring effect when `_speaking` is true.
  - **BLINK**: not found anywhere in `digital_human_avatar_widget.dart` or `viseme_mouth_painter.dart` — likely missing entirely in this fallback renderer.
  - **LISTENING / THINKING / GESTURE**: state setters exist on the controller (`setListening()`, `setThinking()`, `setGesture()`), but I have **not** confirmed whether they produce any visible change in the widget — need to check whether `assetPath` (an `Image.asset` swapped via `ValueKey`) is state-dependent (likely swaps between the reference expression JPGs per persona/state — `skylar_expression_focused.jpg`, `skylar_expression_warm.jpg`, etc. under `assets/digital_human/reference/`), and this absolutely needs physical-device visual confirmation once the device reconnects — per the user's explicit instruction, do **not** mark any of the 7 states PASS without seeing real visible behavior on the Moto G.
  - **Do not redesign or re-rig anything here** — the user was explicit that this investigation must finish before any changes, and that a static asset or a state-variable-changes-in-code is explicitly NOT sufficient evidence of a working feature.
  - Newer/more complete implementation lives in `UrbanGoodz2026-Revised` at `lib/features/digital_human/` (note: different path — `features/digital_human/` not `features/urban_goodz/digital_human/`) — has NOT been traced yet this session. That's the reconciliation source per the user's "CORRECTION" message — do not blindly merge its 189-file diff into the coherent worktree; reconcile deliberately once actually needed.

## Not started yet

- **Track A — Mobile Appium**: CUSTOMER_TEST/VENDOR_TEST/DRIVER_TEST accounts now exist (see the 2026-08-19 evening update #2 section above). **Checked where the existing Appium test infra actually lives — it's in the wrong repo for this session's test environment.** `UrbanGoodz2026-Revised/tests/appium/*.e2e.js` + its `wdio.conf.js` are placeholder-quality (try/catch-and-log-only) as previously noted, but that whole repo is explicitly NOT the execution environment (see "Repositories in play" at the top of this doc — it's missing driver_app and has a gutted vendor_app). **The coherent worktree (`C:\UG\claude-order-anywhere-card-driver`) has no Appium/WebdriverIO setup at all** — confirmed via search, only a Playwright-style `tests/urban_goodz_public_qa.spec.ts` exists there. So "fix the placeholder Appium tests" is not a small task — it's building new wdio/Appium config + page objects from scratch against the correct package names (`com.urbangoodz.customer`, `com.urbangoodz.vendor`, `com.urbangoodz.driver`, all confirmed installed on the Moto G via `adb shell pm list packages`). Not started; flagging the real scope here so the next session doesn't assume it's a quick edit.
- **Track B — Financial/commission reconciliation**: not started. Need to read the actual configured commission/payout rules from the backend (do not invent percentages) and verify against a real order — ideally one created through the actual mobile flow once Track A works, but could start against existing seeded order data in `urban_goodz_local` if useful groundwork now.
- **Cross-role mobile→admin verification**: blocked on Track A producing a real order.
- **Digital Human physical verification**: blocked on device reconnect.

## 2026-08-19 evening update #2 (autonomous continuation)

- **Device reconnected**: Moto G (`ZT42268MG6`) is back in `adb devices` as of this update. Physical-device blocker is lifted.
- **Stale build cleanup**: found 3 concurrent `flutter build apk --debug` processes left running from before the `/clear` (PIDs from 19:01/19:11), all stuck contending on the same Gradle output dir for ~20+ min with zero progress (confirmed via `Get-CimInstance Win32_Process` full command-line inspection). Killed all of them, ran `flutter clean`, and launched a single clean build.
- **First clean-build attempt failed on a genuine machine-resource issue, not code**: `../../runtime/vm/os_thread.cc: 345: error: Could not start thread DartWorker: 22` → cascaded into "Gradle build daemon disappeared unexpectedly". Root cause: **this machine only had 0.87 GB free out of 7.41 GB total RAM** at the time (checked via `Get-CimInstance Win32_OperatingSystem`) — almost certainly from a concurrent, unrelated `flutter build apk` still running for the *other* repo (`UrbanGoodz2026-Revised`, gradle 8.14.2, its own Gradle+Kotlin daemons) left over from earlier in the day, on top of MySQL/PHP/everything else. Both `~/.gradle/gradle.properties` and this project's `android/gradle.properties` already carry deliberately-tuned low JVM memory settings from a **previous** session that hit this exact failure mode (see the comments in those files, and [[gradle-global-props-override-trap]]) — they were not the problem this time; it was genuinely system-wide memory exhaustion. Ran `gradlew --stop` (reported no daemons running — the crash had already killed them), confirmed free memory recovered to 1.82 GB, and re-launched a single fresh `nohup flutter build apk --debug > _customer_build_log.txt 2>&1 &` from `C:\UG\claude-order-anywhere-card-driver`. **Check `_customer_build_log.txt` in that dir for current status before assuming done or stuck** — a Monitor task is watching for `BUILD FAILED|✓ Built|FAILURE:|Exception|PathExistsException|PathNotFoundException|daemon disappeared|DartWorker`. **If this fails again with the same signature, the real fix is closing memory-heavy unrelated apps/builds before retrying, not further Gradle tuning** — this machine's 7.4 GB total RAM is the actual ceiling.

- **Second retry hit a real, separate problem: C: drive was at 0 bytes free out of 225 GB.** Cascaded into `FileSystemException: ... There is not enough space on the disk` across multiple subsystems (Kotlin incremental cache, `.dart_tool/flutter_build/.../app.dill`, then a full `ninja`/CMake failure building `rive_common`'s native Android code) — 3 build failures total, all disk-space symptoms not code bugs. Freed space by deleting `C:\Users\D'Andre Good\.gradle\caches` (11.91 GB, fully safe — Gradle re-downloads/rebuilds on demand; it may also have had corrupt partial writes from the disk-full crash anyway) → **13 GB free / 95% used** afterward. Ran `flutter clean` again to clear any corrupted partial artifacts from the crashed attempt, then retried a third time. See new memory `machine-7gb-ram-concurrent-build-limit` for the RAM half of this pattern; this disk-full episode is a related but distinct constraint on the same small machine — **if disk fills up again, `~/.gradle/caches` is the first safe thing to clear**, and it's worth periodically checking `df -h /c` before kicking off any large build here, not just RAM.
- **New mobile test accounts created** (via `php artisan tinker`, verified with `Hash::check`, safe local-only):
  | Role | Email | Phone | Password | Notes |
  |---|---|---|---|---|
  | CUSTOMER_TEST | `customer_test@urbangoodz.test` | `+15125550100` | `TestPass123!` | New `users` row, id 511, `zone_id=1` (Demo Zone), `status=1`. |
  | VENDOR_TEST | `dandregood@urbangoodzdelivery.com` | (existing) | `TestPass123!` (reset) | **Reused existing vendor id 109** ("Test Test") — it was already the user's own dedicated test vendor with a linked store "testing" (store id 109, status active). Did NOT touch any other vendor row — ids 1 is Sixam Mart's stock demo store (untouched, left as-is), ids 2-108/110+ are **real Urban Goodz businesses and real driver personal accounts** (matching actual restaurant/boutique domains and personal Gmail/Yahoo addresses) — do not repurpose or reset passwords on those. |
  | DRIVER_TEST | `driver_test@urbangoodz.test` | `+15125550101` | `TestPass123!` | New `delivery_men` row, id 13, `zone_id=1`, `status=1`, `active=1`, `application_status=approved`, `is_delivery=1`. |

  All 12 pre-existing `delivery_men` rows have realistic personal names/emails and `status=0` — treat as real pending driver applications, not seed data, except id 1 (`developer developer` / `developer@gmail.com`, zone "Demo Zone") which is the stock template account and was only read, not modified.

- **Track B (commission/settlement reconciliation) — groundwork done, real reconciliation still blocked**: Read `UrbanGoodzCommissionResolver` (`app/Services/UrbanGoodz/UrbanGoodzCommissionResolver.php`) end to end — the resolution precedence (transaction/job override → contract → partner → service → module → zone/market → global fallback → legacy store `comission` column → legacy global `business_settings.admin_commission` → safe failure+admin alert) is well-designed and internally consistent, clamps correctly, rejects fixed-amount rules on the percentage-only legacy path instead of mis-settling. Checked live DB state: `urban_goodz_commission_rules` has 0 active rules, `urban_goodz_dispatch_commissions` and both settlement-snapshot tables (`urban_goodz_settlement_snapshots`, `urban_goodz_financial_settlement_snapshots`) have 0 rows — **nothing has ever actually been settled**, so there is no real order to reconcile against yet. Store 1 (Demo Store)'s `comission` column is NULL → would inherit the legacy global `business_settings.admin_commission` = 10% if an order settled today. Platform fee (`urban_goodz_payment_settings` table, key `platform_fee_percent`) has no owner-configured row either → falls back to `safe_non_live_platform_fee_percent` = 10% (mode is `sandbox`, not `live_controlled`, so this is expected, not an error). **Do not fabricate a settlement via tinker to "test" this** — real reconciliation requires an order to actually flow through Track A's mobile chain first.

- **Digital Human — 3 of the "not started yet" questions from the previous update are now resolved by direct code/asset inspection (coherent worktree `C:\UG\claude-order-anywhere-card-driver`, NOT `UrbanGoodz2026-Revised`)**:
  - **GESTURE: confirmed NO visible effect, with certainty, no device needed.** `DigitalHumanStateModel.resolveState()` (`lib/features/urban_goodz/digital_human/models/digital_human_state.dart`) never reads the `gesture` field at all — only `isSpeaking`/`isListening`/`isThinking`/`emotion` feed into it. `gesture` only flows into `toJson()`/`toRiveInputs()` (dead code path since no `.riv` exists). Calling `controller.setGesture(...)` cannot ever change `_assetForState()`'s output in the fallback renderer. This is a structural fact, not something on-device testing could show differently.
  - **BLINK: confirmed absent, with certainty.** Grepped the entire `lib/features/urban_goodz/digital_human/` directory case-insensitively for "blink" — zero matches anywhere. Not a wiring gap, never implemented in this fallback renderer.
  - **LISTENING / THINKING: confirmed they DO change the visible photo, code + asset level.** `_expressionBucket()` in `digital_human_avatar_widget.dart` maps `DigitalHumanState.listening`→`'listening'` and `.thinking`→`'thinking'`, feeding a distinct `assets/image/personas/{persona}_face_{bucket}.png` through `Image.asset` inside an `AnimatedSwitcher` keyed on the asset path — a real state change. Confirmed all 9 expression PNGs exist per persona (`assets/` declares `assets/image/personas/` in `pubspec.yaml`) and are genuinely distinct, correctly-labeled photos (visually spot-checked `skylar_face_listening.png` vs `_thinking.png` vs `_neutral.png` — different poses/expressions, not the corrupted-mislabeled-layer problem documented in [[digital-human-asset-kit-corrupted]] for the *other* repo's per-part PNGs). **Still want one on-device screenshot per state as final confirmation that `Image.asset` actually paints correctly at runtime** (rules out a bundling/asset-declaration bug), but the code- and asset-level uncertainty from the previous handoff is now closed.
  - Note: the 2026-08-19 Thermion/3D rendering work referenced in the `ai-digital-human-mvp-status` memory happened in **`UrbanGoodz2026-Revised`** (branch `customer-tester-build-sprint`), a *different* codebase from this coherent worktree's simpler 2D-photo fallback described above — no contradiction, they're just two different digital-human implementations, and reconciling them is still explicitly deferred per the original handoff.
  - **Attempted on-device verification with the already-installed APK — inconclusive, APK confirmed stale, do not trust these screenshots.** Launched the already-installed customer app (was already open to the Skylar AI Concierge chat screen) and drove it via `adb shell input tap`/`keyevent` + `exec-out screencap`. Triggering the mic button correctly flipped the status badge text "Skylar is speaking" → "Skylar is listening..." (confirms the controller/state wiring works end to end on-device), **but the character image itself was byte-identical between the two states** (a full-body photo: sunglasses, UG tank top, denim shorts, orange heels, handbag) — this contradicts the code trace above, which predicts a headshot swap between `skylar_face_speaking.png`/`skylar_face_listening.png`. Investigated: that full-body image doesn't match ANY of the 9 `assets/image/personas/{persona}_face_*.png` headshot files (visually confirmed, none are full-body), nor `skylar_avatar.png` (a reference-sheet crop, not this photo either). `git log` on `digital_human_avatar_widget.dart` shows a commit **`4b4e1d0` "Rebuild Monique as a full-screen live avatar hero..."** that plausibly replaced an older full-body-hero rendering system with the current headshot-swap one — so the installed APK (`lastUpdateTime` claimed 2026-08-19 19:16 per `dumpsys package`, but that timestamp is evidently not trustworthy as a freshness signal) is almost certainly compiled from source **older than the current commit**, not a true reflection of the code as it stands now. **Do not cite these 3 screenshots (`dh_screen_00/01/02` in the scratchpad) as evidence of anything about the current codebase.**
  - **Still blocked / next step**: the customer APK build in progress above must finish, then run `adb uninstall com.urbangoodz.customer` **before** installing the fresh APK (to eliminate any doubt about install freshness — don't rely on `flutter install`'s own reinstall-detection), then redo this exact screenshot sequence (speaking → listening → thinking → idle, both personas) against the truly-current build. Only then close out the "no PASS without seeing real visible behavior" requirement for LISTENING/THINKING. BREATHING/SPEAKING pulse-ring were already independently confirmed via original handoff's code read, but re-confirm visually too since the running app clearly wasn't matching source in this attempt.

## 2026-08-19 evening update #3 (build infrastructure — 3 failures, root causes fixed)

The customer APK build failed 3 times in a row after the account/Digital-Human work above, each for a genuine environment reason, not a code bug:

1. **RAM exhaustion**: `error: Could not start thread DartWorker` → Gradle daemon crash. Machine had only 0.87 GB free out of 7.41 GB total — an unrelated `flutter build apk` for a *different* repo (`UrbanGoodz2026-Revised`) had been running concurrently in the background for 25+ min. Fixed: killed it, confirmed with `Get-CimInstance Win32_OperatingSystem`/`gradlew --stop` that memory recovered. **Never run two Flutter/Gradle builds at once on this machine, even across different repos** — see new memory [[machine-7gb-ram-concurrent-build-limit]].
2. **Disk exhaustion**: C: hit 0 bytes free out of 225 GB mid-build → cascading `FileSystemException: not enough space` across Kotlin's incremental cache, `.dart_tool/flutter_build/.../app.dill`, and the native `rive_common` CMake/ninja build. Fixed by deleting `~/.gradle/caches` (11.91 GB, fully safe/regenerable) → 13 GB free.
3. **[[flutter-apostrophe-path-bug]], new scope — breaks native builds, not just `flutter test`.** Got all the way to linking `librive_text.so` (part of `rive_common`, a transitive dependency of the `rive` package already in `pubspec.yaml` even though no `.riv` file exists yet) before `ninja`/`clang++` failed: `error: no such file or directory: 'CMakeFiles/rive_text.dir/C_/Users/DAndre_Good/AppData/Local/Pub/Cache/...`. CMake generated object-file paths from `C:\Users\D'Andre Good\AppData\Local\Pub\Cache\...` and the apostrophe got dropped inconsistently between path generation and the linker's response file, so the `.o` files the linker looks for don't exist at the path it expects. This is a **more serious instance** of the previously-known-`flutter-test`-only bug — it now blocks `flutter build apk` for any project depending on `rive_common` (or any package whose native build references pub-cache-relative paths) on this machine.
   - **Fix applied**: relocated the Dart pub cache to an apostrophe-free path, `C:\pubcache`, via `PUB_CACHE=C:/pubcache flutter pub get` (one-time ~150-package re-download) then `PUB_CACHE=C:/pubcache flutter build apk --debug`, both run from one detached wrapper script so the env var stays set for both steps. **Any future `flutter build`/`flutter run` on this project must set `PUB_CACHE=C:/pubcache` (or set it as a persistent user environment variable) or it will hit this exact link failure again once it reaches `rive_common`.**
   - Build was still running as of this note with the fix applied — check `_customer_build_log.txt` in `C:\UG\claude-order-anywhere-card-driver` for the outcome before assuming success or restarting from scratch.

## 2026-08-19 evening update #4 — CRITICAL: mobile app is hardcoded to production, plus fresh Digital Human on-device results

**Build succeeded.** `flutter build apk --debug` completed with the `PUB_CACHE=C:/pubcache` fix: `✓ Built build\app\outputs\flutter-apk\app-debug.apk`. Confirmed genuinely current via clean install: `adb uninstall com.urbangoodz.customer` then fresh `adb install` → `versionCode=7`/`versionName=3.9.2` (matches `pubspec.yaml` exactly) and `firstInstallTime == lastUpdateTime` (no staleness ambiguity this time, unlike the earlier attempt).

**CRITICAL FINDING — the mobile Customer app cannot be pointed at the local dev backend.** `lib/util/app_constants.dart:52`: `static const String baseUrl = 'https://admin.urbangoodzdelivery.com';` — a hardcoded compile-time constant, no `String.fromEnvironment`/`--dart-define`/flavor mechanism anywhere in `lib/util/`. Every build of this app, debug or release, talks to **real production**, never to `127.0.0.1:8000` or any local server. This means:
- The CUSTOMER_TEST/VENDOR_TEST/DRIVER_TEST accounts created earlier this session (see update #2) live only in the **local** `urban_goodz_local` DB and are invisible to the actual mobile app — confirmed by attempting to log in as `customer_test@urbangoodz.test` through the real UI and it never succeeding (login screen just re-focuses the password field, no navigation, no visible error — consistent with the request going to prod and prod correctly not recognizing the account).
- **Any further interactive testing through this mobile app touches real production** — a real "Sign Up" would create a real customer account visible to the real business; any order placed would be a real order. **I stopped here rather than creating a real production account to keep testing** — that needs an explicit decision from the user (e.g., either accept a real throwaway test signup with a clearly fake email, or accept that mobile authenticated-flow testing on this device is fundamentally blocked without a backend-URL override being added to the app first).
- **This changes the shape of "Track A" entirely.** The `CUSTOMER_TEST`/`VENDOR_TEST`/`DRIVER_TEST` accounts are still useful for verifying backend-side logic (commission resolution, order processing) directly against the local DB/API, but they cannot be used to drive the actual installed mobile app end-to-end as originally envisioned, unless either (a) a real prod account is used instead (needs user sign-off given real-data implications), or (b) someone adds a dev/staging base-URL override to the app (a real code change, not just test setup).

### Digital Human — LISTENING now visually confirmed on the genuinely-current build; THINKING/SPEAKING blocked by the production-auth issue above

Reached the AI Concierge screen via **Menu → scroll down → "Ask UG — Urban Goodz AI Concierge"** tile (not a default landing screen on fresh install as earlier mistakenly assumed — that was residual state from an already-running instance). This is a different/updated screen than what the stale APK showed: header photo is a bust/headshot (not the old full-body sunglasses photo), "Talk to Skylar" button, an "Ask Skylar anything" search bar with its own mic icon, and Quick Option chips (Stylists, Order Anywhere, Track my order, etc.).

- **IDLE**: sharp, in-focus header photo. No mic status indicator. (`state_idle_clean.png`, `skylar_reached.png` in scratchpad `472fe0bc-d535-4a6a-96bb-8dea6e24618c`.)
- **LISTENING — real, reproducible, confirmed on current build**: tapping the search-bar mic icon → Android's mic-permission dialog (granted "While using the app") → header photo **visibly blurs** (out-of-focus effect) and stays blurred for the whole duration the OS-level green mic-recording indicator is active in the status bar; live partial speech-to-text appeared in the search field (picked up the word "things" from ambient room audio). Re-entering the screen fresh reverts cleanly to the sharp idle photo with no mic indicator — confirmed this is a real, state-driven, reproducible effect, not a stuck rendering bug. (`state_listening_clean.png`, `state_after_talk_btn.png`, `state_toggle_off.png`, `state_reentry.png`.) **Note this is a blur filter, not the headshot-swap-between-named-PNGs mechanism predicted from the earlier `_expressionBucket`/`_assetForState` code trace** — either a different code path renders this specific screen than `digital_human_avatar_widget.dart`'s `_buildFallbackFace`, or there's an additional blur-on-listening effect layered on top that wasn't obvious from the earlier read. Worth a follow-up code check to reconcile (not done yet — ran out of time chasing the production-backend blocker instead).
- **THINKING / SPEAKING — still not confirmed.** Submitting a voice query while a Guest produced no visible response at all (image stayed blurred, no chat bubble). Tried a Quick Option ("Stylists") instead — got a clear, real, honest error: the header text changed to **"Something didn't load right -- try me again?"** and a red **"Unauthenticated."** banner appeared. This confirms the AI Concierge backend calls require a logged-in user, and (per the finding above) require a **real production** login — Guest sessions and local-DB test accounts both fail here, for different reasons. **Blocked** until the production-auth question above is resolved by the user.

**Files/evidence**: all screenshots referenced above are in the Bash-tool scratchpad directory `C:\Users\D'ANDR~1\AppData\Local\Temp\claude\C--Users-D-Andre-Good\472fe0bc-d535-4a6a-96bb-8dea6e24618c\scratchpad\` — copy them out if they need to survive past this session's temp cleanup.

## Key gotchas for whoever continues

- This machine's `mysql`/`php`/`flutter`/`adb` are not all on the default Bash PATH — full paths are noted above where relevant.
- Several `.claude/worktrees/...` and `C:\UG\...` paths are **other lanes'** working copies — never edit those, only the two paths named at the top of this doc.
- `.env` in `AdminPanel_Update_V39` has 3 duplicate `SESSION_DRIVER`/`CACHE_DRIVER`/`QUEUE_CONNECTION` blocks (lines ~65-75) — cosmetic mess, not currently breaking anything, flagged but not cleaned up.
- Don't kill MySQL processes without checking with the user first if there's any doubt — earlier in this session a kill was done reflexively and the user corrected it (the DB was genuinely stuck in a boot loop that time, but be careful).
